import {StyleSheet} from "react-native";

export const CommonStyles = StyleSheet.create({
    displayText: {
        gap: 8,
        marginBottom: 8,
        marginTop: 8
    },
    titleContainer: {
        flexDirection: 'row',
        alignItems: 'center',
        gap: 8,
    },
    stepContainer: {
        gap: 18,
        marginBottom: 18,
    }
});